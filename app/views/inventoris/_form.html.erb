<%= form_with(model: inventori) do |form| %>
  <% if inventori.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inventori.errors.count, "error") %> prohibited this inventori from being saved:</h2>

      <ul>
        <% inventori.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block", class: "form-label"  %>
    <%= form.text_field :name, class: "form-control m-2 w-50" %>
  </div>

  <div>
    <%= form.label :description, style: "display: block", class: "form-label"  %>
    <%= form.text_area :description, class: "form-control m-2 w-50 row-5" %>
  </div>

  <div class="mt-4 actions">
    <%= form.submit class:"btn btn-success" %>
  </div>
<% end %>
